export type HomeStep = {
  title: string;
  copy: string;
};

export type CompanionSummary = {
  name: string;
  bio: string;
  specialty?: string;
  rating: string;
  years: string;
  image: string;
  alt?: string;
};
