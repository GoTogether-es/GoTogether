{
  "installCommand": "pnpm install",
  "buildCommand": "pnpm --filter @gotogether/web build",
  "outputDirectory": "apps/web/.next",
  "framework": "nextjs",
  "env": {
    "PRISMA_SKIP_POSTINSTALL_GENERATE": "true"
  }
}
